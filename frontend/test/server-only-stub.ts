/**
 * vitest에서 "server-only"를 대신하는 빈 모듈.
 *
 * 진짜 "server-only"는 브라우저 환경에서 import되면 예외를 던진다. 그게 이
 * 가드의 목적이다 -- 클라이언트 컴포넌트가 서버 전용 모듈을 끌어다 쓰면
 * Next 빌드가 실패한다. 실제로 로그인 화면이 node:crypto를 브라우저로 끌고
 * 들어가 죽은 적이 있어서 붙였다.
 *
 * 그런데 vitest는 jsdom 환경이라 서버 코드를 검사할 때도 브라우저로 해석해
 * 같은 예외를 던진다. 테스트에서만 이 빈 모듈로 바꿔치기한다. Next 빌드의
 * 가드는 그대로 살아 있다.
 */
export {};
